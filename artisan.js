// VS CODE SHORCUTS

// this will open the command pallette
CTRL + SHIFT + P(keyboard shortcut)

// this will open the terminal window
CTRL + `(keyboard shortcut)

// this will toggle your sidebar
CTRL + B(keyboard shortcut)

// this will oopen your file switcher
CTRL + P(keyboard shortcut)

// this will clear your terminal screen
clear


// stop the server
CTRL + C(keyboard shortcut)


// this will run our laravel project on the server
php artisan serve(keyboard shortcut)

// this will create a controler
php artisan make:controller TaskController

// this will list all routes in our application
php artisan route:list --except-vendor

// this will create a resource controller
php artisan make:controller MySampleResourceController --resource

// this will create an api resource controller
php artisan make:controller StudentResourceController --api

// this will create a view from artisan
php artisan make:view info
